{
  "name": "erp-monolith",
  "version": "1.0.0",
  "description": "ERP Monolith API",
  "scripts": {
    "dev": "mvn spring-boot:run",
    "install": "mvn clean install -DskipTests",
    "build": "node scripts/build.js build",
    "docker": "node scripts/build.js docker",
    "clean": "mvn clean && rm -rf release target"
  }
}
